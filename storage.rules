rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Simplified rules for Beta testing to ensure connectivity.
      // This allows both read and write for any user.
      allow read, write: if true;
    }
  }
}
