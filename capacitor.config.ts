import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ahmedarafa.Si-Neuroai',
  appName: 'Si-NeuroAI',
  webDir: 'public',
  server: {
    url: 'https://super-axd.pages.dev',
    cleartext: true
  }
};

export default config;
